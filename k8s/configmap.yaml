apiVersion: v1
kind: ConfigMap
metadata:
  name: customer-support-config
  namespace: customer-support
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "customer_support"
  CORS_ORIGINS: "https://support.taskflow.com"
